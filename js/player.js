function Player(PlayerID, PlayerColor, PlayerName){
    // This is the identifier used in the DB system
    this.ID = PlayerID;

    // Name
    this.Name = PlayerName;

    // This is the color representing this player
    this.Color = PlayerColor;
}
